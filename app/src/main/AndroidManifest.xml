<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 批次3 功能5: 关键提醒全屏弹窗。
         ⚠️ 修正（H2）：Android 14+(API 34) 起 USE_FULL_SCREEN_INTENT 默认仅授予
         电话/闹钟类应用，普通应用需用户手动到设置开启；未授予时 setFullScreenIntent
         / setBypassDnd 会静默降级为普通通知。首次开启关键提醒时引导授权（见 CreateReminderScreen） -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- I6: 关键提醒穿透勿扰（setBypassDnd）需要声明此权限，并在首次开启关键提醒时引导用户到设置授予 -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- v1.8.7 在线升级: 安装 APK 需要用户授权未知来源 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- AI 功能所需权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- v1.9.7 近场传输: 扫码接收需要相机 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".ReminderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ReminderApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ReminderApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- v1.8.7 任务④: .ics 导出分享（仅暴露 cache 目录） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 通知操作按钮接收器 -->
        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:exported="false" />

        <!-- 开机重注册提醒 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 桌面小组件 -->
        <receiver
            android:name=".receiver.ReminderWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_reminder_info" />
        </receiver>

        <!-- 小组件「完成」按钮动作接收器 -->
        <receiver
            android:name=".receiver.WidgetActionReceiver"
            android:exported="false" />
    </application>
</manifest>
